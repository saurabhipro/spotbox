<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <record id="view_account_bnk_stmt_cashbox" model="ir.ui.view">
        <field name="name">account.bnk_stmt_cashbox.form</field>
        <field name="model">account.bank.statement.cashbox</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="start_bank_stmt_ids" invisible="1"/>
                    <field name="end_bank_stmt_ids" invisible="1"/>
                    <field name="currency_id" invisible="1"/>
                    <field name="cashbox_lines_ids" nolabel="1" context="{'default_currency_id': currency_id}">
                        <list editable="bottom">
                            <field name="currency_id" invisible="1"/>
                            <field name="number"/>
                            <field name="coin_value" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            <field name="subtotal" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                        </list>
                    </field>
                    <div>
                        <group>
                            <group class="oe_subtotal_footer oe_right" cols="6">
                                <field name="currency_id" invisible="1"/>
                                <div class="o_td_label"></div>
                                <field name="total" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                            </group>
                        </group>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_account_bnk_stmt_cashbox_footer" model="ir.ui.view">
        <field name="name">account.bnk_stmt_cashbox.form</field>
        <field name="model">account.bank.statement.cashbox</field>
        <field name="priority">1000</field>
        <field name="mode">primary</field>
        <field name="inherit_id" ref="view_account_bnk_stmt_cashbox"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="after">
                <footer>
                    <button string="Confirm" class="btn-primary" special="save" data-hotkey="v"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </xpath>
        </field>
    </record>


    <record id="view_bank_statement_form" model="ir.ui.view">
        <field name="name">account.bank.statement.form</field>
        <field name="model">account.bank.statement</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <form string="Bank Statement">
                <header>
                    <button string="Post" state="open" class="oe_highlight"
                            name="button_post" type="object"/>
                    <button string="Validate" class="oe_highlight"
                            name="button_validate_or_action" type="object"
                            invisible = "'all_lines_reconciled' ==  False or 'line_ids' == [] or 'state' != 'posted' "/>
                    <button string="Reset to New"
                            name="button_reopen" type="object"
                            invisible="state == 'posted'"
                            />
<!--                    attrs="{'invisible': [('state', '!=', 'posted')]}" DONE-->
                    <button string="Reset to Processing"
                            name="button_reprocess" type="object"
                            invisible="state == 'confirm'"
                           />
<!--                     attrs="{'invisible': [('state', '!=', 'confirm')]}" DONE-->
                    <button name="get_transactions" type="object" string="Get Transactions from Plaid"
                            class="oe_highlight"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,posted,confirm"/>
                </header>
                <div class="alert alert-warning" role="alert" invisible="previous_statement_id == 'False' or is_valid_balance_start == 'True'"
                    >
<!--                     attrs="{'invisible': ['|', ('previous_statement_id', '=', False), ('is_valid_balance_start', '=', True)]}" DONE-->
                    The starting balance <b>doesn't match</b> with the ending balance of the previous bank statement.
                </div>
                <sheet>
                    <!-- Invisible fields -->
                    <field name="all_lines_reconciled" invisible="1"/>
                    <field name="move_line_count" invisible="1"/>
                    <field name="country_code" invisible="1"/>

                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" name="button_journal_entries"
                                string="Journal Entries" type="object"
                                invisible="'move_line_count' == 0" icon="fa-bars"
                                />
<!--                        attrs="{'invisible':[('move_line_count','=', 0)]}" DONE-->
                    </div>

                    <div class="oe_title oe_inline">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. BNK/2021/0001"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="previous_statement_id" invisible="1"/>
                            <field name="is_valid_balance_start" invisible="1"/>
                            <field name="journal_type" invisible="1"/>
                            <field name="cashbox_start_id" invisible="1"/>
                            <field name="cashbox_end_id" invisible="1"/>
                            <field name="journal_id" domain="[('type', '=', journal_type)]"
                                   options="{'no_open': True, 'no_create': True}"
                                   readonly="'move_line_count' != 0"/>
<!--                                                               attrs="{'readonly': [('move_line_count','!=', 0)]}"DONE-->

                            <field name="date"

                                   options="{'datepicker': {'warn_future': true}}"
                            readonly="state != 'open'"
                            />
<!--                             attrs="{'readonly': [('state', '!=', 'open')]}"-->
                            <field name='company_id' options="{'no_create': True}" groups="base.group_multi_company"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                        <group>
                            <label for="balance_start"/>
                            <div>
                                <field name="balance_start" class="oe_inline" readonly="state == 'confirm'"
                                     />
<!--                                  attrs="{'readonly': [('state', '=', 'confirm')]}" DONE-->
                                <button name="open_cashbox_id"

                                        string="&#8594; Count" type="object" class="oe_edit_only oe_link oe_inline"
                                        context="{'balance':'start'}"
                                        invisible="state != 'open' or 'journal_type' != 'cash'"
                                />
<!--                                 attrs="{'invisible': ['|',('state','!=','open'),('journal_type','!=','cash')]}" DONE-->
                            </div>
                            <label for="balance_end_real"/>
                            <div>
                                <field name="balance_end_real" class="oe_inline"
                                       readonly="state == 'confirm'"
                                       />
<!--                                attrs="{'readonly': [('state', '=', 'confirm')]}"DONE-->
                                <button name="open_cashbox_id"
                                        invisible="state != 'open' or 'journal_type' != 'cash'"
                                        string="&#8594; Count" type="object" class="oe_edit_only oe_link oe_inline"
                                        context="{'balance':'close'}"/>
<!--                                attrs="{'invisible': ['|',('state','!=','open'),('journal_type','!=','cash')]}"DONE-->
                            </div>
                        </group>
                    </group>

                    <notebook>
                        <page string="Transactions" name="statement_line_ids">
                            <field name="line_ids"
                                   readonly="state != 'open'"
                                   context="{'default_date': date, 'default_journal_id': journal_id}">
<!--                                 attrs="{'readonly': [('state', '!=', 'open')]}" DONE-->
                                <list string="Statement lines"
                                      editable="bottom"
                                      decoration-muted="is_reconciled"
                                      limit="500">

                                    <!-- Invisible fields -->
                                    <field name="company_id" invisible="1"/>
                                    <field name="state" invisible="1"/>
                                    <field name="is_reconciled" invisible="1"/>
                                    <field name="currency_id" invisible="1"/>
                                    <field name="partner_bank_id" invisible="1"/>
                                    <field name="country_code" invisible="1"/>

                                    <!-- Visible fields -->
                                    <field name="sequence" widget="handle"/>
                                    <field name="date"
                                           readonly="'parent.state' != 'open'"
                                           />
<!--                                    attrs="{'readonly': [('parent.state', '!=', 'open')]}"DONE-->
                                    <field name="payment_ref"/>
                                    <field name="partner_id"
                                           readonly="'parent.state' != 'open'"
                                           domain="['|', ('parent_id','=', False), ('is_company','=',True)]"/>
<!--                                    attrs="{'readonly': [('parent.state', '!=', 'open')]}"DONE-->
                                    <field name="ref" optional="hidden"/>
                                    <field name="narration" string="Notes" optional="hidden"/>
                                    <field name="transaction_type" optional="hidden"/>
                                    <field name="amount_currency" optional="hidden" groups="base.group_multi_currency"
                                         readonly="'parent.state' != 'open'" />
<!--                                     attrs="{'readonly': [('parent.state', '!=', 'open')]}"DONE-->
                                    <field name="foreign_currency_id" optional="hidden"
                                           groups="base.group_multi_currency"
                                          readonly="'parent.state' != 'open'"/>
<!--                                     attrs="{'readonly': [('parent.state', '!=', 'open')]}"DONE-->
                                    <field name="amount"
                                          readonly="'parent.state' != 'open'" />
<!--                                    attrs="{'readonly': [('parent.state', '!=', 'open')]}"DONE-->
                                    <field name="account_number" optional="hidden"/>

                                    <!-- Buttons -->
                                    <button name="button_undo_reconciliation" type="object"
                                            invisible="'is_reconciled' == False"
                                            column_invisible="'parent.state' != 'posted'"
                                            string="Revert reconciliation" icon="fa-undo"/>
<!--                                     attrs="{'invisible': [('is_reconciled', '=', False)], 'column_invisible': [('parent.state', '!=', 'posted')]}"DONE-->
                                </list>
                            </field>
                        </page>
                    </notebook>
                    <br/>
                    <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total">
                        <div class="oe_inline">
                            <label for="balance_end"/>
                        </div>
                        <field name="balance_end" nolabel="1"/>
                    </group>
                    <div class="oe_clear"/>
                </sheet>
                <div class="o_attachment_preview"/>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record model="ir.actions.act_window.view" id="action_bank_statement_form_bank">
        <field name="sequence" eval="2"/>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_bank_statement_form"/>
        <field name="act_window_id" ref="account.action_bank_statement_tree"/>
    </record>

    <record id="view_bank_statement_tree_inherit" model="ir.ui.view">
        <field name="name">view.bank.statement.tree</field>
        <field name="model">account.bank.statement</field>
        <field name="inherit_id" ref="account.view_bank_statement_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="create">True</attribute>
            </xpath>
        </field>
    </record>
</odoo>